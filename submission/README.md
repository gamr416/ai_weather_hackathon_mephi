# Пакет для эксперта

См. корневой [`README.md`](../README.md) (раздел «Для экспертов») и [`MANIFEST.json`](MANIFEST.json).

Кратко:

1. Checkpoint: `artifacts/best.pt` (Residual-FSQ, 8.46 M).
2. Метрики: `metrics/*.json` — \(S_\mathrm{all}\approx0.081\), CR ×63, exact roundtrip.
3. σ: `ref_stats/sigma_official_28ch.npz` (LadCast).
4. Пересчёт: команды в корневом README.
